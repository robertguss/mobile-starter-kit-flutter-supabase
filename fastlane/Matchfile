git_url(ENV.fetch("MATCH_GIT_URL", "git@github.com:example/certificates.git"))
storage_mode("git")
type("appstore")
readonly(true)

app_identifier([
  ENV.fetch("FASTLANE_APP_IDENTIFIER", "com.example.flutterSupabaseStarter"),
])
